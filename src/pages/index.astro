---
import Layout from "../layouts/Layout.astro";
import "@fontsource/share-tech-mono";
import "@fontsource/vt323";

import Loader2Icon from "@lucide/svelte/icons/loader-2";
import { Button } from "$lib/components/ui/button/index.js";
import LanguageSelector from "../components/LanguageSelector.svelte";
import ThemeToggle from "../components/ThemeToggle.svelte";
---

<script is:inline>
  const isBrowser = typeof localStorage !== "undefined";
  const getThemePreference = () => {
    if (isBrowser && localStorage.getItem("theme")) {
      return localStorage.getItem("theme");
    }
    return window.matchMedia("(prefers-color-scheme: light)").matches
      ? "dark"
      : "light";
  };
  const isDark = getThemePreference() === "dark";
  document.documentElement.classList[isDark ? "add" : "remove"]("dark");

  if (isBrowser) {
    const observer = new MutationObserver(() => {
      const isDark = document.documentElement.classList.contains("dark");
      localStorage.setItem("theme", isDark ? "dark" : "light");
    });
    observer.observe(document.documentElement, {
      attributes: true,
      attributeFilter: ["class"],
    });
  }
</script>

<Layout>
  
  <header class="w-full flex justify-between items-center py-12">
    <a href="#" class="text-2xl font-vst">zakiel</a>

    <div>Z</div>

    <div class="flex items-center">
      <LanguageSelector client:load />
      <ThemeToggle client:load/>
    </div>
  </header>
  <main class="w-full flex-1">
    <!-- Juan Mercedes, Software Developer , Frontend , Backend -->

    <h1 class="text-xl tracking-wider font-offbit">
      Hi ,i'm Juan <!-- — Web & Software Developer. -->
    </h1>

    <p class="text-base font-offbit mt-2">
      a software developer with stronf focus on producing high quality &
      impactful digital experiencie.
    </p>

    <div class="mt-12 flex flex-col gap-8">
      <section>
        <span class="text-xl font-offbit">Proyects</span>
        <div class="flex flex-col gap-18">
          <div class="w-full flex">
            <div class="flex-1">
              <span>Alcyon</span>
              <p>
                A platform designed for early-stage entrepreneurs to pitch,
                browse, and engage with startup ideas. It’s built to impress
                both users and investors with blazing speed, compelling visuals,
                and a modern tech stack.
              </p>
              <div class="flex flex-col gap-3 mt-6 text-sm font-offbit">
                <span
                  >// Leveraged Partial Prerendering and After for faster
                  loading.
                </span>
                <span
                  >// Simplified idea submission with a clean, intuitive design.
                </span>
                <span
                  >// Enhanced browsing with seamless performance optimization.</span
                >

                <div class="flex mt-4 gap-6">
                  <Button variant="ghost" class="cursor-pointer">
                    <Loader2Icon class="animate-spin" />
                  </Button>
                  <button class="border p-2 cursor-pointer">Github</button>
                </div>
              </div>
            </div>
            <div class="flex-1 border"></div>
          </div>
          <div class="w-full flex">
            <div class="flex-1">
              <span>Alcyon</span>
              <p>
                A platform designed for early-stage entrepreneurs to pitch,
                browse, and engage with startup ideas. It’s built to impress
                both users and investors with blazing speed, compelling visuals,
                and a modern tech stack.
              </p>
              <div class="flex flex-col gap-3 mt-6 text-sm font-offbit">
                <span
                  >// Leveraged Partial Prerendering and After for faster
                  loading.
                </span>
                <span
                  >// Simplified idea submission with a clean, intuitive design.
                </span>
                <span
                  >// Enhanced browsing with seamless performance optimization.</span
                >

                <div class="flex mt-4 gap-6">
                  <button class="border p-2 cursor-pointer">Study case</button>
                  <button class="border p-2 cursor-pointer">Github</button>
                </div>
              </div>
            </div>
            <div class="flex-1 border"></div>
          </div>
        </div>
      </section>

      <section class="mb-12">
        <span class="font-offbit">About me</span>
        <div>
          Lorem ipsum dolor sit amet consectetur adipisicing elit. Voluptate
          dolore ut blanditiis beatae earum cumque repudiandae nisi dolores
          omnis nemo magni placeat, aliquid cupiditate architecto facere id
          consequuntur eius. Ratione.
        </div>
      </section>
    </div>
    <footer class="bg-amber-600 w-full">Footer</footer>
  </main>
</Layout>
